services:
  api:
    image: runtrace-integration-test-api:latest
    build: !reset null
    ports: !override
      - "8100:8000"

  web:
    image: runtrace-integration-test-web:latest
    build: !reset null
    ports: !override
      - "3100:3000"
